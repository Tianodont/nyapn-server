#ifndef MAIN
#define MAIN

#include <stdio.h>
#include <string.h>
#include "argparse/argparse.h"

int cmd_profile(int, const char **);
int cmd_run(int, const char **);
int cmd_stop(int, const char **);
int cmd_settings(int, const char **);

#endif
